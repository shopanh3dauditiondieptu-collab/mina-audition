const box=document.getElementById("blogPosts");const posts=JSON.parse(localStorage.getItem("mina_v2_posts")||"[]");box.innerHTML=posts.length?posts.map(p=>`<article class="post-card"><img src="${p.image||'images/default-post.svg'}"><h3>${p.title}</h3><p>${p.desc||''}</p></article>`).join(""):`<article class="post-card"><h3>Chưa có bài viết</h3><p>Hãy đăng bài trong Admin.</p></article>`;
