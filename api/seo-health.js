const {config,loadSource,setCommonHeaders}=require("./_mina-seo-utils");
module.exports=async function handler(req,res){if(req.method!=="GET"){res.setHeader("Allow","GET");return res.status(405).end()}const result={ok:true,version:"2.0.0",site:config.site.origin,checkedAt:new Date().toISOString(),sources:{}};for(const [name,source] of Object.entries(config.sources)){try{const list=await loadSource(req,source);result.sources[name]={ok:true,count:list.length}}catch(error){result.sources[name]={ok:false,count:0,error:error.message}}}setCommonHeaders(res,"application/json",60);return res.status(200).json(result)};
