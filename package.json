{
  "name": "mina-clean-rebuild",
  "version": "1.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node -e \"const fs=require('fs'); if(!fs.existsSync('public')){throw new Error('Khong tim thay thu muc public o goc repository');} fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('public','dist',{recursive:true}); console.log('Mina Clean build completed');\""
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
