{
  "name": "mina-audition",
  "version": "6.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate:seo": "node scripts/generate-sitemap.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "build": "node scripts/build-site.mjs"
  }
}
