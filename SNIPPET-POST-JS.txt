DÁN 2 DÒNG NÀY VÀO js/post.js

Vị trí: ngay sau khi đã lấy được dữ liệu bài viết:
const p = snap.data();

Thêm:

p.id = postId;
window.MinaSEO?.applyPost(p);

Hoặc dùng sự kiện:

document.dispatchEvent(new CustomEvent("mina:post-loaded", {
  detail: { ...p, id: postId }
}));
